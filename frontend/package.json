{
  "name": "@dronebridge/esp32-frontend",
  "version": "1.0.0",
  "devDependencies": {
    "i": "^0.3.7",
    "inline-source-cli": "^2.0.0",
    "npm": "^11.12.0",
    "shx": "^0.4.0"
  },
  "scripts": {
    "build": "inline-source --compress < index.html > build/index.html && shx cp *.png *.ico build/"
  }
}
